source "https://rubygems.org"

# Use the GitHub Pages bundle (pins Jekyll and supported plugins)
gem "github-pages", group: :jekyll_plugins

# Needed to serve locally on Ruby 3+
gem "webrick", "~> 1.8"
